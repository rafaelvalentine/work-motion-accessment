import styled from "styled-components";
import tw from "twin.macro"

export  const Wrapper = styled.section`
 ${tw`bg-gray-200 h-20 w-full text-gray-500 flex justify-between items-center font-medium px-[10%]`}
`
